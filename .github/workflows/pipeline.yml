name: pipeline

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  quality:
    name: quality
    uses: guilhermelinosp/templates/.github/workflows/lib-quality.yml@main
    with:
      go-version-file: go.mod
